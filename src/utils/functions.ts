export const lastInArray = (arr: Array<any>) =>
{
    return arr[arr.length - 1]
};

export const firstInArray = (arr: Array<any>) =>
{
    return arr[0]
};

export const findInArray = (arr: Array<any>, index: number) =>
{
    return arr[index]
};
